# Data policy

`data/sample_logs/` is generated locally by `python scripts/run_demo_pipeline.py`.

All CSV files are synthetic/demo logs. They are not hardware telemetry and should not be presented as real robot performance evidence.

